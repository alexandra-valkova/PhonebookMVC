@model PhonebookMVC.ViewModels.Phones.PhoneCreateEditVM

<div>
    <h4>@Model.Contact.FirstName's Phone</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.PhoneNumber)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PhoneNumber)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "CreateEdit", new { id = Model.ID }) |
    @Html.ActionLink("Back to List", "Index", new { id = Model.Contact.ID })
</p>
